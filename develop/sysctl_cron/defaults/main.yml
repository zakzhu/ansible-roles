---
# defaults file for sysctl_cron

crontab:
  cron1:
    name: initialize /etc/sysctl.d/60-init-base.conf
    special_time: reboot
    user: root
    job: /bin/bash /etc/sysctl.d/init_base_sysctl.sh &> /dev/null
    cron_file: "ansible_{{ role_name }}"
