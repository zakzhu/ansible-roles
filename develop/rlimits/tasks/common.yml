---
- name: <>
  pam_limits:
    domain: "{{ v['domain'] }}"
    value: "{{ v['value'] }}"
    limit_item: "{{ v['item'] }}"
    limit_type: "{{ v['type'] }}"
    dest: "{{ v['file'] }}"
  loop: "{{ limits | dict2items }}"
  vars:
    v: "{{ item.value }}"
