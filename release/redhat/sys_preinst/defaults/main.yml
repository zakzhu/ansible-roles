---
# defaults file for sys_preinst

# FROM TASK: <r_sys_preinst_t_common> preinstall the base packages
sys_preinst_pkgs:
  cloud:
    rpm:
      - "@Base"
      - "@Debugging Tools"
      - "@Development Tools"
      - "@Large Systems Performance"
      - "@Networking Tools"
      - "@Performance Tools"
      - "@System Administration Tools"
      - lrzsz
      - nmap
      - telnet
      - tree
    deb:
  bare_metal:
    rpm:
    deb:
