---
- name: <r_users_t_cr8_sgrps> create secondary groups
  group:
    name: "{{ v.name }}"
    gid: "{{ v.gid }}"
    state: present
  loop: "{{ secondary_groups | dict2items }}"
  vars:
    v: "{{ item.value }}"
